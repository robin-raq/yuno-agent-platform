# Build Log — Yuno Agent Orchestration Platform

One line per meaningful decision or phase transition. This is what you read instead of being interrupted.

## 2026-06-15

- **Intake.** PRD = `project_yuno.pdf` ("AI Agent Orchestration Platform" hiring take-home). Confirmed greenfield (prior session notes claimed built code that isn't on disk).
- **Framing.** "Yuno" = the payments company issuing the challenge. Remittance / Cross-Border Payout flow will be the domain-themed multi-agent **workflow template** (one of the PRD's required 2+), riding on the generic orchestration engine — NOT an architecture change. Payouts simulated; mock FX + sanctions data.
- **Decision — runtime:** Goose (native Anthropic provider; real tool loops + token tracking; built-in scheduler/sessions). Per PRD "must integrate one of OpenClaw/OpenCode/Goose."
- **Decision — agent LLM:** Anthropic (`ANTHROPIC_API_KEY`). TTS narration → OpenAI if key present, else macOS `say`.
- **Decision — deploy:** Railway (persistent container suits long-lived runtime + SSE + Telegram). Also runs fully local via one setup command (PRD requirement). Vercel rejected: serverless can't host a persistent agent runtime.
- **Preflight:** PASS git/gh(robin-raq)/node23/npm11/railway/vercel/ffmpeg. Browser automation via agent-browser + Chrome MCP (shell check false-negative). AI key pending intake.
- **Scaffold:** git init (branch `main`), dirs `docs/ design/ ui/`, `.gitignore`, `.env.example`. Local only — no remote yet.
- **Phase 1 done:** `docs/PRD-expanded.md` — 8-screen inventory (Dashboard, Agents, Agent Editor, Workflows, Builder, Runs, Run View, Channels), SQLite data model, 2 AI runtime surfaces (workflow-node exec + Telegram turn), 2 templates (Cross-Border Payout + Dev Pipeline). One open question (Goose invocation mode) deferred to a Phase-4 smoke test; non-blocking.
- **Phase 2 done:** captured y.uno via agent-browser. Measured tokens → `design/style-guide.md` (+ `design/home.png`, `home-full.png`). Yuno system: Titillium Web, light-weight large headings, indigo #3E4FE0 primary, lime #E0ED80 accent, lavender tints, hairline #ECEFF2 borders, 8/16px radii, floating cards. Bonus: their hero is literally a node-graph orchestration diagram — on-brand for our workflow canvas.
- **Phase 3 done:** `ui/mockup.html` — single self-contained file (48 KB), all 8 screens, wired show/hide nav (verified all 7 nav routes + sub-route runview), agent-editor tabs, SVG workflow graph with approval gate + feedback/approve/complete edges, Telegram phone mock. Verified standalone in browser (`design/mockup-shots/`). Realistic remittance data (sanctions hit, over-cap → approval gate). Nothing out of scope.
- **GATE — credentials.** Phases 1–3 needed no secrets; Phase 4 (runtime) does. Paused for: funded ANTHROPIC_API_KEY, Telegram BotFather token + chat_id, Stripe TEST payment provider token, optional OpenAI key (TTS). Repo create / push / Railway deploy will ALSO pause for explicit approval (outward-facing + irreversible) per safety rules — the local TDD build runs autonomously between gates.
- **Scope add — Telegram Payments pay-in (TEST).** User opted to bake in a real in-chat invoice (Stripe TEST provider token) as the funding leg: FX Quote → Collect (`sendInvoice`/`successful_payment`) → approval gate → simulated payout; unpaid/timeout → refund + notify. No real money; payout stays simulated (Telegram is a pay-in rail, not a disbursement rail). Synced into `docs/PRD-expanded.md` (scope, `Payment` entity, integrations, template-A graph, acceptance), `ui/mockup.html` (Telegram invoice/paid, Channels log, Run View Collect step + trail, builder Collect node), and `.env.example` (`TELEGRAM_PAYMENT_PROVIDER_TOKEN`).
- **Scope add — Evals (harness + Evaluations screen).** Yuno values evals; PRD's Key Impact Metrics already imply them (task completion rate, A2A reliability). Added: an `evals/` harness (~10 golden scenarios → real-engine replay → deterministic + LLM-judge scorer) wired into `verify.sh`, plus a **9th screen** `/evals` (scorecard + A/B config experiment). No new credentials (judge reuses ANTHROPIC_API_KEY). Synced into PRD-expanded (scope, screen inventory → 9, `EvalScenario`/`EvalResult`/`Experiment` entities, AI surface #3 LLM-judge, acceptance) + mockup (nav item, Evaluations section, router). LLM-judge = 3rd AI runtime surface (offline, not on live path).

## 2026-06-15 — Phase 4 begins

- **Credentials verified** (names/lengths only): ANTHROPIC_API_KEY, OPENAI_API_KEY, TELEGRAM_BOT_TOKEN present. Bot = **@raq_yuno_bot** (validated via getMe). `TELEGRAM_CHAT_ID` pending (user to DM the bot; gateway also captures it from inbound). Payment provider token **deferred per user** ("add all except payment token") → pay-in built but gated off when token absent.
- **Goose smoke test PASSED** — de-risks the runtime. `GOOSE_PROVIDER=anthropic GOOSE_MODEL=claude-haiku-4-5-20251001 ANTHROPIC_API_KEY=… goose run -t '…' --no-session --max-turns 1` returned a real completion. Env-var config works (no keyring / `goose configure`). config.yaml absent and not needed.
- **Open question RESOLVED** — integrate via `goose run` subprocess per task (recipes for tools); parse stdout (strip banner up to "goose is ready"). ACP `serve` reserved for a streaming enhancement.
- **Stack (library choices — skill says my call):** TypeScript end-to-end; **Fastify + better-sqlite3 + SSE** (server); **React + Vite** (web); **Vitest** (tests); native `fetch` (node 23) for Telegram. Local-first one-command run + Railway deploy. Runtime via `tsx`; `tsc --noEmit` kept as the typecheck gate (a green boot ≠ a green build). Justification → README.
- **S1 spine — GREEN.** Scaffolded TS project; `npm install` (193 pkgs). Built: `src/config.ts` (env + capability flags), `src/domain/types.ts`, `src/db/` (better-sqlite3 + schema + `makeAgentsRepo` factory for `:memory:` testability), `src/runtime/goose.ts` (`runGooseTask` subprocess + pure `stripBanner`), `src/routes/` (health + agents CRUD w/ zod), Fastify `buildServer(db)`. Tests 9/9 (stripBanner cases + agent repo/API via `app.inject`). `scripts/verify.sh` = typecheck + test (+ web build when present) → **VERIFY OK**. Not yet committed (awaiting commit-cadence OK; never pushing without approval). `npm audit`: 10 vulns in transitive deps — noted, not force-fixed (would break); revisit before ship.
- **Next (S2):** data model + workflow graph engine (conditional edges, feedback-loop cap), A2A message bus + persistence, a Run executing real Goose tasks, then SSE, Telegram gateway, approval gate, templates, pay-in (gated), eval harness, web UI (9 screens).
